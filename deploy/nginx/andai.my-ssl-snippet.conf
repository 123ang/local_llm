# Optional reference: after `certbot --nginx`, your site file will look like this.
# Certbot usually edits the file in /etc/nginx/sites-available/andai.my automatically.
#
# Typical HTTPS server block (paths from Let's Encrypt):

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name andai.my www.andai.my;
#
#     ssl_certificate /etc/letsencrypt/live/andai.my/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/andai.my/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection $connection_upgrade;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 120s;
#         proxy_send_timeout 120s;
#         proxy_read_timeout 120s;
#     }
#
#     client_max_body_size 50M;
# }
#
# server {
#     listen 80;
#     server_name andai.my www.andai.my;
#     return 301 https://$host$request_uri;
# }
